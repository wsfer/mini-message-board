<!DOCTYPE html>
<html lang="en">
<%- include("partials/head", { title }) %>
<body>
    <% messages.map(({ text, user, added }) => { %>
        <p><%= user %></p>
        <p><%= text %></p>
        <p><%= added %></p>
    <% }) %>
</body>
</html>